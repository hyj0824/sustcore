内核启动代码

负责在内核启动时进行必要的初始化工作, 其保证进入内核主函数时, 一定满足以下几个需求:
1. 构建原始内核页表, 保证进入内核主函数时, 内核虚拟地址空间已经被映射

此处要求内核虚拟地址空间的范围为:

```
0xffff_ffff_0000_0000 ~ 0xffff_ffff_ffff_ffff
```

物理地址到内核虚拟地址的映射关系为:

```
vaddr = paddr + 0xffff_ffff_0000_0000
```